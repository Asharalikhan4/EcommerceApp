import { FC } from "react";
import { View, Text } from "react-native";

const CartScreen: FC = () => {
    return (
        <View>
            <Text>
                Cart Screen
            </Text>
        </View>
    );
};

export default CartScreen;